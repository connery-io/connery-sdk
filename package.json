{
  "name": "connery",
  "private": true,
  "packageManager": "pnpm@7.15.0",
  "scripts": {
    "dev": "dotenv -- turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "turbo": "latest"
  }
}
